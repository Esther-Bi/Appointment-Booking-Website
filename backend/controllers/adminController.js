// API fot adding service
const addService = async (req,res) => {
    try{
        const { name,type,about,available,fees,slots_booked } = req.body
        
    } catch (error){

    }
}

export {addService}